# Patch: Make auth.providers.guest config visible to frontend
# Reason: Aligns guest auth provider config visibility with other authentication providers
# Upstream: https://github.com/backstage/backstage/pull/35766 (or similar)
# Remove when: Upstream commit c9fe33ced6dbcc962667c4d285939d0e11f2435c or later is in the release
--- a/plugins/auth-backend-module-guest-provider/config.d.ts
+++ b/plugins/auth-backend-module-guest-provider/config.d.ts
@@ -18,6 +18,7 @@ export interface Config {
   /** Configuration options for the auth plugin */
   auth?: {
     providers: {
+      /** @visibility frontend */
       guest?: {
         /**
          * The entity reference to use for the guest user.
