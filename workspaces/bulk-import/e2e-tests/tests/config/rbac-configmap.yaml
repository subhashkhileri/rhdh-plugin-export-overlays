kind: ConfigMap
apiVersion: v1
metadata:
  name: rbac-policy
  labels:
    backstage.io/kubernetes-id: developer-hub
data:
  rbac-policy.csv: |
    g, user:default/rhdh-qe-2, role:default/bulk-importer
    p, role:default/bulk-importer, bulk-import, use, allow
    p, role:default/bulk-importer, catalog-entity, read, allow
    p, role:default/bulk-importer, catalog.entity.create, create, allow
    p, role:default/bulk-importer, catalog.location.create, create, allow
    p, role:default/bulk-importer, scaffolder-template, read, allow
    p, role:default/bulk-importer, scaffolder-action, use, allow
    p, role:default/bulk-importer, scaffolder.task.create, create, allow
    p, role:default/bulk-importer, scaffolder-task, read, allow
