# this file could contain configuration that is used across all tests in a
# workspace, it is optional and overrides configuration that's defined in
# the repo root "smoke-tests" folder

# This value should override the root config if present.
app:
  title: Backstage Test App (ACR Workspace Override)

